@model IEnumerable<rthowell.net__ASP_MVC_.Models.PrimerManufacturers>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MfgName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Type)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MfgName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Type)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PrimerId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PrimerId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PrimerId })
        </td>
    </tr>
}

</table>
